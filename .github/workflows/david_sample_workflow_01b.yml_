# Base action, runs on every push in the main branch
name: hello-world_on_push
on: 
  push:
    branches: [main]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
  